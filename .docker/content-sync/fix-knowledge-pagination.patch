diff --git a/pkg/openwebui/client.go b/pkg/openwebui/client.go
index 1234567..abcdefg 100644
--- a/pkg/openwebui/client.go
+++ b/pkg/openwebui/client.go
@@ -XX,XX +XX,XX @@ type Knowledge struct {
 	// ... existing fields
 }
 
+// KnowledgeListResponse represents the paginated response from OpenWebUI API
+type KnowledgeListResponse struct {
+	Items []*Knowledge `json:"items"`
+	Total int          `json:"total"`
+}
+
 // ListKnowledge lists all knowledge sources
 func (c *Client) ListKnowledge(ctx context.Context) ([]*Knowledge, error) {
 	req, err := http.NewRequestWithContext(ctx, "GET", c.baseURL+"/api/v1/knowledge/", nil)
@@ -XX,XX +XX,XX @@ func (c *Client) ListKnowledge(ctx context.Context) ([]*Knowledge, error) {
 		return nil, fmt.Errorf("failed to list knowledge: %w", err)
 	}
 
-	var knowledge []*Knowledge
-	if err := json.NewDecoder(resp.Body).Decode(&knowledge); err != nil {
+	var response KnowledgeListResponse
+	if err := json.NewDecoder(resp.Body).Decode(&response); err != nil {
 		return nil, fmt.Errorf("failed to decode response: %w", err)
 	}
 
-	return knowledge, nil
+	return response.Items, nil
 }